import { AuthPageClient } from "@/components/AuthPageClient";

// This is a Server Component — the interactive logic lives in the Client Component below
export default function HomePage() {
  return <AuthPageClient />;
}
